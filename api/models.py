"""
Models for the API app.

Note: The API app primarily uses models from the core app.
This file exists to ensure Django properly recognizes the API app.
"""

# Import models from the core app if needed
# from core.models import NewsSource, Category, Article, ScrapingLog